---
"@justeattakeaway/pie-webc-core": minor
---

[Added] - `listItemLabelContext` and the `ListItemControlLabel` type, so a `pie-list-item` can provide its accessible name and description to a slotted control (which applies them to the element that carries its semantics) via `@lit/context`.
