---
"@justeattakeaway/pie-list": minor
---

[Added] - `pie-list-item` now also supports being used inside a `pie-checkbox-group`, and provides its accessible name and description to the slotted control via context so the control can name itself (a radio applies it to its host; a checkbox to its internal input).
