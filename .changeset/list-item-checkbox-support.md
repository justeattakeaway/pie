---
"@justeattakeaway/pie-list": minor
---

[Added] - `pie-list-item` supports `selectionType="checkbox"` for use inside a `pie-checkbox-group`: it names the slotted checkbox from its text (applied to the checkbox's internal input) and forwards row clicks to it.
