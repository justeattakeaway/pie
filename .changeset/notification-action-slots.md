---
"@justeattakeaway/pie-notification": minor
"@justeattakeaway/pie-storybook": patch
---

[Added] - Support for `leadingAction` and `supportingAction` slots, allowing consumers to pass custom `pie-button` elements for full control over action rendering (e.g. icons, loading states, disabled states)
