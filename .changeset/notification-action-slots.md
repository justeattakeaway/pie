---
"@justeattakeaway/pie-notification": minor
---

[Added] - Optional `leadingAction` and `supportingAction` named slots for custom `pie-button` elements, enabling icons, loading states, and other button features on notification action buttons
