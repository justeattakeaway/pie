---
"@justeattakeaway/pie-checkbox-group": minor
---

[Added] - Support for using `pie-list-item` as children of `pie-checkbox-group`. Checkboxes can be wrapped in list items (at any nesting depth) to render a divided, list-style checkbox group; the group manages the list items' roles and accessibility, and selection stays independent per checkbox.
