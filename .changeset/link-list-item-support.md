---
"@justeattakeaway/pie-list": minor
---

[Added] - `isLink` on `pie-list-item`: slot an empty `<a slot="link" href="...">` to turn the whole row into a single navigation link. The anchor is stretched over the entire row and named from the item's text (`primaryText` as the accessible name, `secondaryText`/`metaText` as the description).
