---
"@justeattakeaway/pie-toast": minor
"@justeattakeaway/pie-toast-provider": minor
---

[Added] - `aria` prop to `pie-toast` for setting a custom `aria-label` on the close button via `aria.close`
[Added] - `pie-toast-provider` now passes the `aria` prop down to the rendered `pie-toast`
