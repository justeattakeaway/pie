---
"@justeattakeaway/pie-switch": minor
---

[Added] - When used inside a `pie-list-item` (`selection-type="switch"`), `pie-switch` takes its accessible name and description from the item (via context, applied to its internal input) so the whole row names and toggles the switch, and its track tints in step with the row's hover and active states.
