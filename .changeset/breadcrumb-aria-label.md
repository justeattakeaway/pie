---
"@justeattakeaway/pie-breadcrumb": minor
---

[Added] - `aria` prop support on `pie-breadcrumb` and `pie-breadcrumb-item` components, allowing consumers to set a custom `aria-label` on the nav element and individual item links
