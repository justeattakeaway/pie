---
"@justeattakeaway/pie-button": patch
"@justeattakeaway/pie-cookie-banner": patch
"@justeattakeaway/pie-modal": patch
"@justeattakeaway/pie-notification": patch
---

[Fixed] - Applied `width: 100%` to the `pie-button` host element when `isFullWidth` is set, ensuring buttons span full width in flex, grid, and block layouts.
[Updated] - `pie-cookie-banner`, `pie-modal`, and `pie-notification` to reset button width on desktop.
