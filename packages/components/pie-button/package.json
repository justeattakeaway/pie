{
  "name": "@justeattakeaway/pie-button",
  "customElements": "custom-elements.json",
  "version": "0.10.2",
  "description": "PIE design system button built using web components",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "scripts": {
    "build": "yarn create-react-wrapper && run -T vite build && yarn remove-wrapper",
    "create-react-wrapper": "cd ../../tools/pie-wrappers-react && yarn build:wrapper",
    "remove-wrapper": "cd ../../tools/pie-wrappers-react && yarn remove:wrapper",
    "watch": "run -T vite build --watch",
    "dev": "run -T vite",
    "test": "echo \"Error: no test specified\" && exit 0",
    "test:browsers": "npx playwright test -c ./playwright-lit.config.ts",
    "test:browsers:ci": "yarn test:browsers",
    "test:visual": "run -T cross-env-shell PERCY_TOKEN=${PERCY_TOKEN_PIE_BUTTON} percy exec --allowed-hostname cloudfront.net -- npx playwright test -c ./playwright-lit-visual.config.ts",
    "test:visual:ci": "yarn test:visual"
  },
  "author": "JustEatTakeaway - Design System Web Team",
  "license": "Apache-2.0",
  "volta": {
    "extends": "../../../package.json"
  },
  "dependencies": {
    "@custom-elements-manifest/analyzer": "^0.8.0",
    "@lit-labs/react": "1.1.1",
    "cross-env": "^7.0.3",
    "custom-elements-manifest": "^2.0.0",
    "dotenv": "^16.0.3",
    "fs-extra": "^11.1.1",
    "pascal-case": "^3.1.2",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "vite": "^4.3.1"
  }
}
