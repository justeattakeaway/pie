{
  "name": "@justeattakeaway/pie-card",
  "version": "0.27.2",
  "description": "PIE Design System Card built using Web Components",
  "repository": {
    "type": "git",
    "url": "https://github.com/justeattakeaway/pie",
    "directory": "packages/components/pie-card"
  },
  "homepage": "https://webc.pie.design/?path=/docs/components-card--overview",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "custom-elements.json",
    "src",
    "dist",
    "**/*.d.ts"
  ],
  "pieMetadata": {
    "componentStatus": "beta",
    "replaces": {
      "snacks": [
        "Card"
      ]
    }
  },
  "scripts": {
    "build": "run -T vite build",
    "build:react-wrapper": "run -T build-react-wrapper",
    "create:manifest": "run -T cem analyze --litelement",
    "lint:scripts": "run -T eslint .",
    "lint:scripts:fix": "yarn lint:scripts --fix",
    "lint:style": "run -T stylelint ./src/**/*.{css,scss}",
    "lint:style:fix": "yarn lint:style --fix",
    "watch": "run -T vite build --watch",
    "test:browsers": "run -T playwright test -c ./playwright-lit.config.ts",
    "test:browsers:ci": "yarn test:browsers",
    "test:visual": "run -T cross-env-shell PERCY_TOKEN=${PERCY_TOKEN_PIE_CARD} percy exec --allowed-hostname cloudfront.net -- run -T playwright test -c ./playwright-lit-visual.config.ts",
    "test:visual:ci": "yarn test:visual"
  },
  "author": "Just Eat Takeaway.com - Design System Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "@justeattakeaway/pie-components-config": "0.21.3",
    "@justeattakeaway/pie-css": "1.1.3",
    "@justeattakeaway/pie-monorepo-utils": "0.9.4",
    "@justeattakeaway/pie-wrapper-react": "0.14.5"
  },
  "dependencies": {
    "@justeattakeaway/pie-webc-core": "14.0.2"
  },
  "customElements": "custom-elements.json",
  "sideEffects": [
    "dist/*.js"
  ]
}
