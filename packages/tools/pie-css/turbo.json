{
  "$schema": "https://turborepo.org/schema.json",
  "extends": [
    "//"
  ],
  "pipeline": {
    "build": {
      "cache": true,
      "inputs": ["$TURBO_DEFAULT$", "./css/**", "./scss/**"],
      "dependsOn": [
        "^build"
      ]
    }
  }
}
